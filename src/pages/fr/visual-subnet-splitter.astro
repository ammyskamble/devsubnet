---import { type SupportedLanguage } from '../../utils/i18n';

import Layout from '../../layouts/Layout.astro';
import MainShell from '../../components/MainShell.astro';
import { translations } from '../../utils/i18n';

const t = translations.fr;

const lang = (Astro.currentLocale as SupportedLanguage) || 'en';
---

<Layout 
  lang={lang}
  title="Diviseur de sous-réseau visuel et partitionneur CIDR | DevSubnet"
  description="Divisez visuellement des blocs réseau en sous-réseaux. Affichage dynamique de l'arborescence et exportation IaC pour Terraform et Ansible."
>
  <MainShell activeTab="splitter" navPath="visual-subnet-splitter" lang={lang} />
</Layout>
